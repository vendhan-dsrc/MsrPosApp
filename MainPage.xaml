<Page
    x:Class="MsrPosApp.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MsrPosApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel Padding="20" Spacing="10">
            <TextBlock Text="UWP POS MSR Debugger" FontSize="24" FontWeight="Bold" Margin="0,0,0,10"/>

            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="Device Info &amp; Capabilities:" FontWeight="SemiBold" VerticalAlignment="Center"/>
                <Button Content="Copy Device Info" Click="CopyStatus_Click" Margin="20,0,0,0" Padding="10,4,10,4"/>
            </StackPanel>

            <ScrollViewer Height="150" BorderBrush="Gray" BorderThickness="1">
                <TextBlock x:Name="StatusText" TextWrapping="Wrap" Padding="5"/>
            </ScrollViewer>

            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,15,0,0">
                <TextBlock Text="Latest Swipe Data Output:" FontWeight="SemiBold" VerticalAlignment="Center"/>
                <Button Content="Copy Swipe Output" Click="CopySwipe_Click" Margin="20,0,0,0" Padding="10,4,10,4"/>
            </StackPanel>

            <ScrollViewer Height="200" BorderBrush="Gray" BorderThickness="1" Background="#F0F0F0">
                <TextBlock x:Name="SwipeOutput" TextWrapping="Wrap" Padding="5" Foreground="Black" FontFamily="Consolas"/>
            </ScrollViewer>
        </StackPanel>
    </Grid>
</Page>
